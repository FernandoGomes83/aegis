# {{i18n.artifact_titles.design}} — {{project.name}}

> Formalism level: **Formal**
> Generated by the Aegis framework on {{generated_at}}.
> Derives from: `requirements.md`

---

## {{i18n.section_titles.overview}}

{{design.overview.summary}}

### Stack

| Dimension       | Choice                        |
|-----------------|-------------------------------|
| Language        | {{stack.language}}            |
| Runtime         | {{stack.runtime}}             |
| Key Libraries   | {{stack.libraries}}           |
| Data Store      | {{stack.data_store}}          |
| Deployment      | {{stack.deployment}}          |

---

## {{i18n.section_titles.architecture}}

> All major flows and non-trivial component interactions must be documented
> with Mermaid diagrams. Every diagram must have a title and a brief caption.

### High-Level Architecture

```mermaid
{{architecture.high_level_diagram}}
```

> Caption: {{architecture.high_level_caption}}

{{#each architecture.sequence_flows}}
### Sequence: {{this.title}}

```mermaid
{{this.diagram}}
```

> Caption: {{this.caption}}

{{/each}}

### Directory Structure

```
{{architecture.directory_structure}}
```

---

## {{i18n.section_titles.components}}

> Full interface definitions in the project language (code blocks).
> Each component includes: responsibility, typed interface, dependencies,
> and Implements references to PROP-NNN or REQ-NNN.

{{#each components}}
### {{this.name}}

**Responsibility**: {{this.responsibility}}

**Interface**:

```{{../stack.language_id}}
{{this.interface_code}}
```

**Dependencies**: {{this.dependencies}}
**Implements**: {{this.implements}}

{{/each}}

---

## {{i18n.section_titles.data_models}}

> Complete entity definitions with field names, types, constraints, indexes,
> and foreign key relationships. For SQL systems, use CREATE TABLE equivalents.
> For NoSQL, provide the document schema with required/optional annotations.

{{data_models.definitions}}

---

## {{i18n.section_titles.correctness_properties}}

> Exhaustive PROP-NNN entries grouped by functional area.
> Every REQ-NNN must be covered by at least one property.
> Every property that describes a fallible operation must include an explicit
> error-handling clause for each named error condition.

{{#each properties.functional_areas}}
### {{this.area_name}}

{{#each this.properties}}
**{{this.id}}: {{this.name}}**
{{i18n.labels.derives_from}}: {{this.derives_from}}
Priority: {{this.priority}}
Description: {{this.description}}
{{#if this.error_handling}}
Error handling:
{{this.error_handling}}
{{/if}}

{{/each}}
{{/each}}

---

## {{i18n.section_titles.error_handling}}

> General error-handling strategy for the system, followed by specific cases
> not already covered inside individual PROP-NNN entries above.

### General Strategy

{{error_handling.general_strategy}}

### Specific Cases

{{#each error_handling.specific_cases}}
**{{this.scenario}}**
Trigger: {{this.trigger}}
System response: {{this.response}}
Logged: {{this.logged}}
Retryable: {{this.retryable}}

{{/each}}

---

<!-- AUTO-GENERATED SECURITY PROPERTIES — do not edit this section manually.
     Re-run `/aegis:design` to refresh. Source: aegis/framework/security/security-properties.yaml -->

## Security Properties

> The following SEC-PROP-* entries are automatically injected by the Aegis framework.
> They are present at every formalism level and cannot be removed or suppressed.
> Each entry derives from a SEC-REQ-* in `requirements.md`.
> At Formal level, each SEC-PROP includes an explicit error-handling clause.

{{#each properties.security}}
**{{this.id}}: {{this.name}}**
{{i18n.labels.derives_from}}: {{this.validates}}
Statement: {{this.statement}}
{{#if this.error_handling}}
Error handling:
{{this.error_handling}}
{{/if}}

{{/each}}

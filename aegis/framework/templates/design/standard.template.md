# Technical Design — {{project.name}}

> Formalism level: **Standard**
> Generated by the Aegis framework on {{generated_at}}.
> Derives from: `requirements.md`

---

## Overview

{{design.overview.summary}}

### Stack

| Dimension       | Choice                        |
|-----------------|-------------------------------|
| Language        | {{stack.language}}            |
| Runtime         | {{stack.runtime}}             |
| Key Libraries   | {{stack.libraries}}           |
| Data Store      | {{stack.data_store}}          |
| Deployment      | {{stack.deployment}}          |

---

## Architecture

> High-level component diagram and directory structure.

{{architecture.diagram}}

### Directory Structure

```
{{architecture.directory_structure}}
```

---

## Components

> For each major component: responsibility, public interface (inputs/outputs),
> dependencies, and Implements references to PROP-NNN or REQ-NNN.

{{#each components}}
### {{this.name}}

**Responsibility**: {{this.responsibility}}

**Interface**:

| Operation          | Input                | Output               |
|--------------------|----------------------|----------------------|
{{#each this.interface}}
| {{this.operation}} | {{this.input}}       | {{this.output}}      |
{{/each}}

**Dependencies**: {{this.dependencies}}
**Implements**: {{this.implements}}

{{/each}}

---

## Data Models

> Full entity definitions with field names, types, and constraints.
> Relationships between entities must be explicit.

{{data_models.definitions}}

---

## Correctness Properties

> PROP-NNN entries grouped by functional area.
> Cover all critical behavioral guarantees — no fixed upper limit.
> Each property must reference the requirement(s) it derives from.

{{#each properties.functional_areas}}
### {{this.area_name}}

{{#each this.properties}}
**{{this.id}}: {{this.name}}**
Derives From: {{this.derives_from}}
Description: {{this.description}}

{{/each}}
{{/each}}

---

<!-- AUTO-GENERATED SECURITY PROPERTIES — do not edit this section manually.
     Re-run `/aegis:design` to refresh. Source: aegis/framework/security/security-properties.yaml -->

## Security Properties

> The following SEC-PROP-* entries are automatically injected by the Aegis framework.
> They are present at every formalism level and cannot be removed or suppressed.
> Each entry derives from a SEC-REQ-* in `requirements.md`.

{{#each properties.security}}
**{{this.id}}: {{this.name}}**
Derives From: {{this.validates}}
Statement: {{this.statement}}

{{/each}}

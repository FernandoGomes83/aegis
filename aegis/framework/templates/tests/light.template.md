# Test Specification — {{project.name}}

> Formalism level: **Light**
> Generated by the Aegis Framework — review before use.
> This artifact was automatically generated by the Aegis framework and should be reviewed before use.

---

## Test Strategy

**Stack**: {{design.stack}}

Test approach: basic e2e + essential property tests.

- End-to-end tests cover the happy path and at least one error path per feature.
- Property-based tests cover the 1–3 correctness properties defined in `design.md`.
- Security tests (TEST-SEC-*) are auto-injected alongside regular tests — one per SEC-PROP-* entry.
- No edge-case matrix required at this level.

> This section was automatically reviewed for security implications; manual review is still recommended for sensitive contexts.

---

## Pre-Implementation Tests

Write these tests **before** implementing the corresponding code (RED phase).
All entries below must fail on an empty codebase and pass once the implementation is complete.

### Property-Based Tests

<!-- Add one TEST-PROP-NNN block per PROP-NNN entry in design.md (1–3 at Light level). -->
<!-- Security property tests (TEST-SEC-*) are appended automatically after this section. -->

**TEST-PROP-001: {{test.prop.001.title}}**
Type: Property-based
Validates: {{test.prop.001.validates}}
Description: {{test.prop.001.description}}
Assertion: {{test.prop.001.assertion}}

---

### End-to-End Tests

<!-- Add one TEST-E2E-NNN block per feature happy path plus at least one error path. -->

**TEST-E2E-001: {{test.e2e.001.title}}**
Type: E2E
Validates: {{test.e2e.001.validates}}
Steps:
{{test.e2e.001.steps}}
Expected: {{test.e2e.001.expected}}

---

### Security Tests (auto-injected)

<!-- TEST-SEC-* entries are generated automatically from SEC-PROP-* entries in design.md.  -->
<!-- Do not add or remove them manually. Re-run `/aegis:tests` to regenerate this section. -->

> Reference: SECURITY_UNIVERSAL §14 checklist must be verified for every feature before
> it is declared production-ready. See `aegis/framework/security/SECURITY_UNIVERSAL.md`.

---

## Per-Task Tests

<!-- List every TASK-NNN from tasks.md that will receive unit tests during implementation. -->
<!-- At Light level, a single bullet per task is sufficient — no subtask breakdown required. -->

- TASK-001: {{task.001.label}} — unit tests written during implementation
- TASK-002: {{task.002.label}} — unit tests written during implementation
<!-- Add one line per task. -->
